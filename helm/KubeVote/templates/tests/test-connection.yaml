apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "KubeVote.fullname" . }}-test-connection"
  labels:
    {{- include "KubeVote.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "KubeVote.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
