apiVersion: v1
kind: Service
metadata:
  name: {{ include "KubeVote.fullname" . }}
  labels:
    {{- include "KubeVote.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "KubeVote.selectorLabels" . | nindent 4 }}
